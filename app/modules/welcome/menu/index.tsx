import { motion } from "framer-motion";
import { memo } from "react";

function Menu() {
  return (
    <motion.section
      id="concept"
      className="h-[100vh] w-[100vw] bg-white flex shadow-[0_-25px_50px_-12px_rgb(0,0,0,0.25)] gap-10"
    >
      asdfasdf
    </motion.section>
  );
}

export default memo(Menu);
